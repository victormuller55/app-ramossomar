name: app_ramos_candidatura
description: "Ramos - Candidatura"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  muller_package:
    path: ../muller_package

  flutter:
    sdk: flutter

  bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  shared_preferences: ^2.2.3
  http: ^1.6.0
  http_parser: ^4.1.2
  image_picker: ^1.2.1
  mask_text_input_formatter: ^2.9.0
  url_launcher: ^6.3.1
  path_provider: ^2.1.5
  share_plus: ^10.1.4
  open_filex: ^4.7.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo_ramos_somar.png
